import { PassingDirective } from './passing.directive';

describe('PassingDirective', () => {
  it('should create an instance', () => {
    const directive = new PassingDirective();
    expect(directive).toBeTruthy();
  });
});
